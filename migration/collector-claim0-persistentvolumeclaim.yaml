apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: collector-claim0
  name: collector-claim0
  namespace: httpr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
